{
  "name": "@popcorn-log/worker",
  "version": "0.1.0",
  "private": true,
  "description": "popcorn-log 无状态转发层：TMDB 代理 + 海报转发 + 坚果云 WebDAV 转发",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
